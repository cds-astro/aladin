package cds.allsky;

public enum Action {
   
   INDEX, TILES, JPEG, MOC, MOCINDEX, ALLSKY, GZIP, GUNZIP,
   FINDER,  // Pour compatibilité
   CLEAN, CLEANINDEX, CLEANTILES, CLEANFITS, CLEANJPEG,
   RGB
	
}
